{
  "name": "database",
  "version": "1.0.0",
  "main": "none",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "docker build .",
    "setup": "docker run --name=numerical-mysql -p 3306:3306 -p 33060:33060 -d mysql/mysql-server",
    "grepForPassword": "docker logs numerical-mysql 2>&1 | grep GENERATED",
    "ports": "docker port numerical-mysql",
    "interactive": "docker exec -it numerical-mysql mysql -uroot -p",
    "start": "docker start numerical-mysql",
    "stop": "docker stop numerical-mysql",
    "remove": "docker rm numerical-mysql"
  }
}
