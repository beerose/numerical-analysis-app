{
  "name": "common",
  "version": "1.0.0-alpha.0",
  "scripts": {
    "build": "tsc --build . && cp ./package.json ../../dist/common",
    "tsc": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^3.5.1"
  },
  "dependencies": {
    "io-ts": "^2.0.0",
    "nom-ts": "^0.1.0"
  }
}
